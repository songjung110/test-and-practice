import MainController from './controllers/MainController.js';

document.addEventListener('DOMContentLoaded', () => {
    // document 가 로드완료되면 MainController init을 ,,, 탐
    MainController.init();
});
